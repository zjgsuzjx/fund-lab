# Backend reads these settings from the root .env; nonempty environment variables win.
# psql does not load this file; supply connection options and enter its password prompt.
PGHOST=127.0.0.1
PGPORT=5432
PGDATABASE=simulate_alipay
PGUSER=postgres
# Fill in locally. Never commit the real password.
PGPASSWORD=
